用户当前正在编辑此文件：{{filename}}
文件内容如下：
```swift:{{filename}}
{{filecontent}}
```

用户已从该文件中选中了以下代码：
```swift
{{selected}}
```

用户的请求是：

请提供一个关于如何使用 `{{selected}}` 的简短示例。

- 只返回一个代码块。
- 不要使用注释。
- 不要使用 print 语句。
- 不要导入任何额外的模块。
