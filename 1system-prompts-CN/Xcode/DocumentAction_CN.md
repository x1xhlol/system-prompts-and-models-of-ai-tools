用户当前正在编辑此文件：{{filename}}
文件内容如下：
```swift:{{filename}}
{{filecontent}}
```

用户已从该文件中选中了以下代码：
```swift
{{selected_code}}
```

用户的请求是：

为 `{{selected_code}}` 提供文档注释。

- 只返回一个代码块。
- 只包含文档注释，不要包含其他 Swift 代码。
